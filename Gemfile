source "https://rubygems.org"

# GitHub Pages (includes supported Jekyll plugins)
gem "github-pages", group: :jekyll_plugins

# Pagination support (compatible with GitHub Pages' supported plugin)
gem "jekyll-paginate", group: :jekyll_plugins

# Required for `bundle exec jekyll serve` on newer Ruby versions
gem "webrick", platforms: :ruby
gem "csv", platforms: :ruby
gem "bigdecimal", platforms: :ruby
